# pnpm config
# Allow build scripts for trusted packages (prisma, esbuild) to run on install.
enable-pre-post-scripts=true
# Keep a strict, isolated node_modules (default). Hoist only types for editor DX.
public-hoist-pattern[]=*@types*
