# PROGRESS — Veriterra

État vivant du projet. Mis à jour à chaque tranche/story.

## Où on en est

**Correctifs carte (retour porteur) : LIVRÉE (`fix/carte-camera-mobile`, PR à venir).** Deux points. (1) **La bascule Plan/Satellite réinitialisait le zoom et l'angle 3D.** Deux causes : l'effet d'ensoleillement remettait la carte à plat (`easeTo pitch 0`) dans sa branche « pas d'analyse », or cet effet re-tourne à chaque `mapReady` (donc à chaque bascule de fond) => `wasSunActiveRef` pour ne remettre à plat **qu'en SORTIE d'analyse** ; et `switchBasemap` **capture la caméra** (center/zoom/pitch/bearing) avant `setStyle(diff:false)` et fait un `jumpTo` après chargement (le style plan chargé par URL appliquait son center/zoom par défaut). (2) **Ensoleillement inutilisable sur téléphone** (recherche + sliders plein écran) : état `isNarrow` (`matchMedia` 640px), la **recherche est masquée pendant l'analyse** sur petit écran, et le **panneau d'ensoleillement devient repliable** (replié par défaut sur mobile : heure seule ; « Plus d'options » déplie saison + repères + détails). Grand écran strictement inchangé. Revue adversariale (correction/régression carte, mobile) : **3 findings corrigés** (2 réfutés) : (MEDIUM) le panneau replié masquait l'**indisponibilité de source** (bâti/végétation) => condition étendue pour la garder visible même repliée (règle 3) ; (LOW) bascule de fond **pendant le chargement** de l'analyse re-cadrait la vue => `sunFramedKeyRef` pour ne pas re-cadrer un centroïde déjà cadré (la caméra restaurée survit) ; (LOW documenté) cliquer « Quitter l'analyse » pile pendant le rechargement de style provoque un bref rebond 3D->plat (fonctionnellement correct, timing rare). Vérifié : typecheck, lint, tests (app 168), build verts.

**Pipeline de statuts, US-5.1 : LIVRÉE (`feat/statuts-pipeline`, PR à venir).** Le suivi CRM passe de 4 statuts portefeuille à un **pipeline de démarches à 7 états** : À contacter, À visiter, Visité, Démarches en cours, Sous compromis, **Vendu**, **Écarté** (le terminal « Vendu ou écarté » du backlog **séparé** en deux, choix porteur ; défaut `A_CONTACTER`). **Migration** : enum Prisma `TerrainStatus` recréé (nouveau type + `ALTER COLUMN USING` + drop/rename, en une transaction, sans changement RLS) et **tous les terrains existants réinitialisés à `A_CONTACTER`** (choix porteur assumé, opération à sens unique documentée dans l'en-tête). **Vocabulaire recentré** sur `modules/terrains/status.ts` comme **source unique** (clés, libellés, pins, badges, ordre, **couleur hex**) : `TERRAIN_STATUSES` (allowlist serveur) et `STATUS_COLORS` (pins carte) en **dérivent** (ré-export), `dashboard-map` utilise `statusMeta().color`, `StatusPin` (@veriterra/ui) étendu aux 7 états **avec repli neutre** pour un statut inconnu (règle 3, ne plante plus). **Changement de statut rapide depuis la fiche ET le dashboard** via un composant partagé `StatusChanger` (menu des 7 états, `PATCH /api/terrains/[id]` scopé `forOrg`/RLS puis `router.refresh()`) : dans l'**en-tête de fiche** (le `<select>` de l'édition est retiré) et dans **chaque ligne du tableau** (ligne restructurée en « lien étiré » pour garder la navigation tout en rendant le changeur interactif sans imbriquer un bouton dans un `<a>`). Revue adversariale multi-lentilles (migration/isolation, interaction/régression, règles/vocabulaire) : **4 findings confirmés corrigés** (2 réfutés : fenêtre de rollout = séquencement de déploiement pas un bug SQL ; table de couleurs StatusPin identique à `status.ts`). (MEDIUM) le changement de statut inline **remontait toute la carte** du dashboard (effet de création dépendant de `featureCollection`) => création **une seule fois** + mise à jour des sources par `setData` sans recadrer (préserve pan/zoom). (MEDIUM) le menu du changeur **s'ouvrait uniquement vers le bas** => bascule vers le haut si peu de place + hauteur bornée avec défilement interne (tous les états atteignables même en bas de fenêtre). (LOW) cliquer la ligne pour fermer le menu **naviguait** aussi => **fond invisible** qui absorbe le clic. (LOW) **focus perdu** après sélection => focus rendu au déclencheur. Vérifié : typecheck, lint, tests (app 168 dont `status`, ui 60 dont `status-pin`, db 22 isolation), build verts ; migration appliquée en base et test d'intégration `terrains-service` au vert (create -> A_CONTACTER, update -> A_VISITER, cross-org no-op). **Hors périmètre (fast-follow)** : changement de statut par clic sur un pin de la carte, filtre par statut, journal d'audit.

**Outils de mesure v2 (retour porteur) : LIVRÉE (`feat/mesure-v2`, PR à venir).** Trois manques à l'usage de US-1.5 corrigés. (1) **Étiquettes vivantes sur l'axe + ruban élastique** : le curseur devient le dernier sommet provisoire du brouillon et la longueur de chaque segment s'affiche **sur l'axe** en direct (calque symbole `measure-labels`, police IGN). Rendu **impératif coalescé sur `requestAnimationFrame`** (mousemove -> `renderMeasureNow` lisant des refs), **sans re-render React par pixel**. (2) **Mesures multiples sur le même plan** : modèle `measurements[]` (figées) + `draftPoints` (brouillon) ; distance/surface finalisées au double-clic/« Terminer », dénivelé auto-finalisé après 2 clics + altitudes RGE ALTI, recul finalisé au clic ; « Effacer » (brouillon), « Tout effacer », compteur « N mesures ». (3) **Coexistence avec l'ensoleillement** : l'exclusivité mesure/soleil (ajoutée en revue v1) est **levée** (retour porteur) ; `raiseMeasureLayers` remonte les calques mesure **au-dessus des extrusions 3D** (`moveLayer`) pour que le tracé et surtout les étiquettes restent lisibles sur le bâti. Revue adversariale multi-lentilles (correction/perf, interaction-carte, régression/règles) : **4 findings LOW confirmés corrigés** : le mousemove reposait toute la source même sans ruban possible (garde recul/brouillon vide) ; le dénivelé traçait un **segment fantôme** au-delà des 2 points et une **étiquette de distance nue trompeuse** (règle 3, surtout hors couverture) => dénivelé traité à part (ruban A->curseur seulement pour placer B, **pas d'étiquette de distance**, le Δ n'apparaît qu'à la finalisation) ; l'**étiquette vivante** du brouillon était masquée par la collision au profit des figées => `symbol-sort-key` (brouillon prioritaire) ; ruban périmé d'un tracé à l'autre => curseur effacé au vidage du brouillon. Vérifié : typecheck, lint, tests (**app 164** dont `measure` 26), build verts. **Hors périmètre (fast-follow)** : suppression/édition d'une mesure figée par clic.

**Audit sécurité A→Z du codebase applicatif : FAIT. Correctifs HIGH + MEDIUM LIVRÉS, LOW documentés.** Audit multi-lentilles (6 lentilles : isolation/RLS, authn/authz, injection/SSRF, upload/stockage, web/XSS/secrets, API/DoS/IDOR), chaque finding **vérifié adversarialement** (réfutation par défaut) : **6 findings confirmés** (1 HIGH, 2 MEDIUM, 3 LOW ; 2 réfutés : injection d'URL DVF sans impact, amplification buildings à bbox déterministe).
- **HIGH corrigé (`fix/invitation-email-verifie`, PR #43, mergée)** : l'**acceptation d'invitation au login faisait confiance à un e-mail NON vérifié** (bris d'isolation multi-tenant, règle 2). Le callback jwt passait `profile.email` brut à `bootstrapUserOrganisation`, qui créait une `Membership` avec le rôle de l'invitation (**ADMIN possible**) sans vérifier `email_verified` : un attaquant avec un jeton OIDC asserant `email=victime` + `email_verified=false` rejoignait l'org de la victime. **Même frontière de confiance déjà durcie pour l'admin plateforme, oubliée ici.** Correctif : `bootstrapUserOrganisation` reçoit `emailVerified` et ne traite l'e-mail (rattachement d'invitation **et** persistance `user.email`) que s'il est vérifié ; test de garde à DB réelle (invitation PENDING + login non vérifié ⇒ aucune adhésion, invitation reste PENDING).
- **MEDIUM corrigés (`fix/rate-limit-ecritures-couteuses`, PR à venir)** : (M1) l'**upload de documents** et (M2) le **POST `/enrich`** n'avaient **aucune limite de débit** (contrairement aux proxies géo `/api/buildings` et `/api/vegetation`) ; un membre authentifié pouvait saturer la mémoire du conteneur (OOM, DoS tous tenants) ou la file/les 5 workers partagés + amplifier les appels vers DVF/Géorisques/IGN/Overpass (bannissement IP). Correctif : `withinOrgRateLimit` **généralisé** (extrait dans `lib/rate-limit`, paramètre `limit`, ré-exporté par `bbox-request` pour ne rien casser) et appliqué à `/enrich` (**10/min/org**) et à l'upload (**20/min/org**), renvoi **429** au-delà. Test unitaire (Redis mocké). Le refactor plus profond de l'amplification mémoire de l'upload (streaming direct vers MinIO au lieu du re-parsing multipart bufferisé 3-4x) reste un **fast-follow** (le débit borne déjà l'abus).
- **LOW corrigés (`fix/durcissement-low`, PR à venir)** : (L1) le compose **dev** publiait MinIO/Postgres/Redis sur `0.0.0.0` (**prod OK**, interne) → ports **liés à `127.0.0.1`** (accès hôte conservé, exposition LAN supprimée ; les conteneurs joignent par nom de service, inchangé) ; clé MinIO non-root scopée = durcissement ultérieur noté. (L2) **aucun en-tête de sécurité** → **`headers()` dans `next.config.ts`** : `X-Frame-Options: DENY` (anti-clickjacking, sans conflit avec la CSP stricte propre à la route de download), `nosniff`, `Referrer-Policy`, HSTS ; CSP complète des pages (script-src/`frame-ancestors`) notée comme durcissement ultérieur (à valider contre MapLibre/Next). (L3) upload sans quota → **quota de stockage agrégé par organisation** `S3_ORG_QUOTA_MB` (défaut 5120 Mo, 0 = illimité), vérifié avant `putObject` via un total courant **scopé RLS** (`forOrg`), renvoi **413** au-delà ; cœur pur `wouldExceedOrgQuota` testé.

**Outils de mesure sur la carte, US-1.5, + cadastre visible plus tôt, US-1.12 : LIVRÉE (`feat/outils-mesure`, PR #42, mergée et déployée).** **US-1.5** : 4 outils dans `SelectionMap` (donc dispo à la fois dans l'explorer ET dans la carte de fiche en `readOnly`, la mesure étant en lecture seule) : **Distance** (polyligne, mètres, géodésique Turf), **Surface** (polygone, m² + périmètre), **Dénivelé** (2 points, altitudes **RGE ALTI IGN** appelées **directement depuis le navigateur** comme `fetchParcelleAtPoint`, endpoint public sans clé, CORS ouvert ; Δaltitude + distance + pente % ; hors couverture NODATA -99999 => « indisponible », jamais 0, règle 3), **Recul** (distance la plus courte du point cliqué au **contour cadastral** de la parcelle sélectionnée, `@turf/nearest-point-on-line` sur la géométrie **déjà en state**, aucun refetch). Cœur **pur et testé** `app/src/lib/geo/measure.ts` (23 tests : longueur, aire, pente, recul, NODATA, auto-intersection). Modèle d'interaction : le clic en mode mesure **intercepte avant le garde `readOnly`** (marche donc dans la fiche) ; **fall-through** recul->sélection quand aucune parcelle n'est sélectionnée (sinon on serait bloqué) ; double-clic ou « Terminer » finalise ; Échap efface. Rendu : source/calques `measure-*` réinstallés au `styledata` (survivent à la bascule de fond), tracé teal distinct de l'ambre (sélection) et de l'indigo (surface). Chaque valeur porte sa méthode/source (règle 1). **US-1.12** : les tuiles **vectorielles** PCI d'IGN **ne portent aucune parcelle sous z15** (plancher du jeu de tuiles, vérifié via `metadata.json`), donc baisser le `minzoom` seul afficherait du vide => ajout d'une **sous-couche RASTER** `CADASTRALPARCELS.PARCELLAIRE_EXPRESS` (même fournisseur `data.geopf.fr`, sans clé) visible ~z13-15, **masquée à z15+** où le vectoriel interrogeable prend le relais (handoff net). **Quick wins du backlog déjà livrés (PR #18)** : US-5.2 (infobulle des pins, encore fonctionnelle, rien à faire), US-1.10, US-5.9 ; **inv-2** (sélecteur multi-org) différé (décision d'archi : org active en session). **Revue adversariale multi-lentilles** (correction/régression, règles 1/3/4/5, interaction-carte/a11y, isolation) : **7 findings confirmés corrigés** (3 MEDIUM, 4 LOW), 4 réfutés. (MEDIUM) le dédoublonnage du **double-clic** exigeait une égalité **stricte** des coordonnées ; un double-clic réel dérive de 1 à 3 px, laissant un **sommet parasite** qui gonflait la distance/surface (règle 1) => retrait **inconditionnel** du dernier sommet. (MEDIUM) la surface d'un **polygone auto-intersectant** affichait une **aire algébrique** trompeuse (nœud papillon => ~0) présentée comme mesure ferme => détection `@turf/kinks`, « Tracé invalide (arêtes croisées) » (règle 3). (MEDIUM) la colonne d'outils bas-gauche **sans hauteur bornée** + les panneaux mesure/ensoleillement empilés **débordaient hors écran** sur petit viewport => `maxHeight`+`overflow` **et** mesure/ensoleillement rendus **mutuellement exclusifs** (ce qui règle aussi l'occultation du tracé par le bâti 3D). (LOW) `doubleClickZoom` restait actif en dénivelé/recul (un double-clic zoomait ET posait deux points confondus => « 0 m » trompeur) => désactivé pour **tout** outil de mesure. (LOW x2) le raccourci **Échap** global effaçait la mesure même en tapant dans la recherche d'adresse => ignoré quand le focus est dans un champ. Réfutés : absence d'optional chaining sur `elevations` (même issue capturée), « confiance » d'une mesure vs bloc de données (catégories d'UI distinctes, la source est bien affichée), `dist` Turf toujours renseigné. Vérifié : typecheck, lint, tests (**app 150** dont `measure` 23), build verts ; e2e mesure (frontière auth + bascule du panneau en session).

**Section admin plateforme, US-8.1 (lecture + monitoring) : LIVRÉE (`feat/admin-section`, PR à venir).** Zone `/admin` **séparée** (groupe de routes `(admin)`, shell propre) listant **comptes** et **organisations** de toutes les organisations et un **monitoring de base** (compteurs org/comptes/terrains/documents + volume, invitations en attente ; santé DB/Redis/worker). **Deux tensions traitées avec soin.** (1) **Nouveau tier d'autorisation** distinct du rôle d'org (`OWNER/ADMIN/MEMBER`, scopé organisation) : signal **`platformAdmin`** dérivé d'une **allowlist d'e-mails `ADMIN_EMAILS`** (env serveur, règle 6), stampé dans le JWT à chaque requête (re-validé comme le rôle). **Choix assumé** : allowlist env plutôt qu'une colonne/table, donc **aucune migration** et surtout **aucune table non-tenant** (qui échapperait au garde RLS). (2) **Traversée délibérée de l'isolation (règle 2)** pour lister toutes les orgs/comptes : seul chemin cross-tenant = le client privilégié `admin` (contourne la RLS), **gardé par `requirePlatformAdmin`** (layout `(admin)` + chaque page, défense en profondeur ; anonyme → `/sign-in`, non-admin → `notFound()` 404). Lien « Administration » dans la barre, visible seulement pour un admin plateforme. Revue adversariale (isolation-authz / correction-régression / règles) : **3 findings confirmés corrigés** : (MEDIUM) l'éligibilité admin faisait confiance à l'e-mail OIDC **sans vérifier `email_verified`** (un IdP à e-mail modifiable aurait permis d'usurper une adresse de l'allowlist) → n'accepter que l'e-mail **vérifié**, stocké dans un champ dédié (jamais l'e-mail d'affichage) ; (MEDIUM) la sonde Redis réutilise la connexion BullMQ (offline queue, `maxRetriesPerRequest: null`) et **bloquait indéfiniment** si Redis tombait, gelant `/admin` pile pendant la panne → borne par timeout (course), retombe en « indisponible » ; (LOW) `Promise.all` couplait compteurs et santé, un échec DB faisait planter toute la page → découplé, compteurs en « indisponible » et le panneau de santé s'affiche toujours (règle 3). Vérifié : lint, typecheck, tests (app 127 dont `platform-admin`), build, e2e admin (frontière auth) verts. **Hors périmètre** : mutations (désactiver/octroyer), profondeur des files BullMQ, journal d'audit.

**PWA installable + coquille hors-ligne, US-6.1 (partielle) : LIVRÉE (`feat/pwa-installable`, PR à venir).** L'app devient **installable** : `app/manifest.ts` (nom, `standalone`, `start_url /dashboard`, thème `#2f3b6e`, icônes **192/512/maskable** plus **apple-touch**, toutes générées depuis la marque cadastrale en plein cadre), export `viewport` (theme-color) et `appleWebApp` dans le layout racine, **service worker** écrit à la main (`public/sw.js`) enregistré en production, et une **page de repli `/offline`** neutre. **Périmètre hors-ligne EXPLICITE et borné** (risque R12, règles 1 et 2) : le SW est **same-origin uniquement** (les tuiles carto cross-origin ne sont jamais mises en cache), ne met **JAMAIS** en cache `/api/*` ni le **HTML authentifié** (données sourcées/datées + isolation tenant sur un cache partagé par le profil de navigateur) ; navigations en **réseau d'abord** avec repli sur `/offline` ; seuls les **assets statiques sans donnée de tenant** sont mis en cache. Le proxy sert désormais **publiquement** les assets PWA et la marque (un 302 casserait l'enregistrement du SW, `cache.addAll`, le manifeste et le logo de `/sign-in`), avec des exclusions **ancrées** et **points échappés** (jamais de route tenant exposée par sur-correspondance). Version du cache SW dérivée d'un **identifiant de build** (bust à chaque déploiement). Revue adversariale (PWA/sécurité-isolation/règles) : **4 findings confirmés corrigés** : (MEDIUM) empoisonnement de cache, la marque `/veriterra-mark.svg` était auth-gatée et le SW mettait en cache le HTML de la redirection sous l'URL du SVG (fix : marque publique + garde `!response.redirected`, corrige aussi un logo cassé pré-existant sur `/sign-in`) ; (LOW) cache SW jamais invalidé (VERSION figée) => bust par identifiant de build ; (LOW) exclusions du matcher non ancrées/points non échappés (footgun d'isolation) => ancrage `(?:/|$)` + échappement ; (LOW) e2e ne testait pas le repli hors-ligne => test `setOffline` réel ajouté. Vérifié : lint, typecheck, tests (app 120 dont `manifest`), build, **e2e PWA (5 tests, dont le repli hors-ligne réel via le SW)** verts. **Suite (lot séparé)** : la **consultation hors-ligne des fiches déjà chargées** (US-6.1) demande une décision d'isolation (purge du cache à la déconnexion ou cache par utilisateur) avant de mettre en cache du HTML de tenant.

**Passe responsive / mobile (retour porteur « l'encart prend toute la place sur le téléphone ») : LIVRÉE (`feat/responsive-mobile`, PR à venir).** Toute l'app reste utilisable au doigt sous 640px (point de rupture `sm`), le bureau strictement inchangé. (1) **Barre de navigation** : la nav inline et le cluster compte passent en `hidden sm:flex` et se replient dans un **menu burger** (`< sm`) accessible (bouton `aria-expanded`/`aria-controls`, fermeture sur navigation, Échap et clic extérieur, **focus rendu au déclencheur** sur Échap, motif **disclosure** avec liens natifs et non `role=menu`). (2) **Encart de création (explorer)** : sur mobile c'est une **feuille inférieure** plafonnée à `70dvh` (poignée + repli), et **repliée elle devient une pastille en bas à droite** qui **libère la carte et ses outils** (fond, « Analyser l'ensoleillement ») ; sur `sm+` c'est le panneau flottant d'origine (identique). (3) **Onglets** de la fiche : `overflow-x-auto` (défilement horizontal des libellés longs) avec 2px de marge verticale pour ne pas rogner l'anneau de focus. (4) **Liste « Mes terrains »** : cartes empilées sur mobile via l'astuce `sm:contents` (zéro duplication ; les colonnes du tableau sont **exactement** restituées sur `sm+`, `min-w` re-scopé en `sm:min-w-[640px]`) plus un **tri mobile** (select + sens) puisque les en-têtes cliquables sont masqués. (5) Détails : rangée de boutons d'édition en `flex-wrap`, cible tactile du chevron portée à 44px, carte du tableau de bord recalée sous l'en-tête (`top-[3.625rem]`). Nav « Profil » désormais active sur `/profil` (cohérence avec Explorer/Mes terrains, changement bureau assumé). Revue adversariale multi-lentilles (responsive/a11y/régression) : **7 findings confirmés corrigés** (rognage anneau de focus des onglets, focus non rendu sur Échap, cible 36px, anti-patron `role=menu`, pastille sans `aria-expanded`, régression bureau `min-w` re-scopée en `sm:`, Profil actif assumé) ; 1 réfuté. Vérifié : lint, typecheck, tests (app 118, ui, worker), build verts.

**Carte de la fiche = moteur de l'explorer, focalisé (retour porteur) : LIVRÉE (`feat/fiche-carte-ensoleillement`, PR à venir).** L'onglet **« Soleil » devient « Ensoleillement »** et son ancienne vue 3D isolée (`SunMap`) est remplacée par **le moteur exact de l'explorer** (`SelectionMap`), en **lecture seule** et centré sur la/les parcelle(s) du terrain : même fond plan/satellite, relief permanent, surcouche cadastre et **analyse d'ensoleillement en place** (bascule de fond incluse, corrigée au lot précédent). `SelectionMap` est **paramétré** (props `initialSelection`, `readOnly`, `onExpand`, callbacks rendus optionnels) : en `readOnly` la recherche d'adresse/surface est masquée, le clic n'édite pas, le reset est caché, et la vue **se cadre sur les parcelles** à l'ouverture (`fitBounds`, via `boundsOfGeometries` pur et testé). Un **bouton « agrandir » (icône 4 coins)** ouvre l'explorer plein écran **focalisé sur le terrain** (`/terrains/nouveau?terrain=<id>`) : la page précharge les parcelles (route scoped RLS), les centre, **masque le panneau de création** (pas de doublon) et affiche une barre **« Retour à la fiche »** ; `useSearchParams` est sous `Suspense`. L'explorer normal (sans `?terrain`) reste strictement identique (gate `focusReady` initialisé paresseusement pour éviter tout flash). Revue adversariale : 2 findings confirmés corrigés (double flash de chargement sur l'explorer normal ; e2e du parcours mis à jour, l'ancien `soleil.spec` gardait une route morte). `sun-map.tsx` et `/api/terrains/[id]/buildings` deviennent orphelins (retrait possible plus tard). Vérifié : typecheck, lint, tests (app 118 dont `bbox.test`), build verts ; e2e du parcours (auth) en CI.

**Invitations multi-membres, inv-1 (US-0.4 collaboration) : LIVRÉE (`feat/invitations-membres`, PR à venir).** Le porteur ne pouvait « ajouter aucun e-mail dans l'organisation ». Nouveau modèle **`Invitation`** (organisationId, email, role, status PENDING/ACCEPTED/REVOKED, invitedByUserId, acceptedByUserId, acceptedAt) + migration + **RLS ENABLE/FORCE/policy** (couverte par le garde « toute table à organisationId a RLS »). **Flux** : un OWNER/ADMIN invite un e-mail (pré-autorisation) depuis le bloc Organisation du profil (formulaire e-mail + rôle Membre/Administrateur, liste des invitations en attente révocables, liste des membres avec changement de rôle et retrait) ; au **login** l'e-mail OIDC est comparé (minuscules) aux invitations PENDING et, si correspondance, le **bootstrap** (voie privilégiée `admin`) crée l'adhésion + marque ACCEPTED ; **l'invité atterrit dans l'org d'invitation** (adhésion la plus ancienne, départage par id). Service via **forOrg/withOrg** (jamais `admin` pour les données scopées). **Invariants** : seul un OWNER gère/crée un OWNER (anti-escalade), jamais rétrograder/retirer le **dernier OWNER** (verrou `FOR UPDATE` sur l'organisation, anti-TOCTOU). Routes `/api/org/invitations`, `/api/org/invitations/[id]`, `/api/org/members/[userId]`, gardées OWNER/ADMIN, orgId = session. **Revue adversariale (sécurité/correction) : 5 findings confirmés corrigés** : (HIGH) le JWT figeait `{orgId, role}` au login, si bien qu'un membre retiré gardait l'accès et un OWNER rétrogradé ses pouvoirs jusqu'au re-login => **re-validation VIVANTE de l'adhésion/rôle à chaque requête** (`refreshTenantContext` dans le callback `jwt`, une lecture indexée par requête, on renonce à l'optimisation « zéro aller-retour ») ; (MEDIUM) TOCTOU dernier OWNER => verrou d'organisation ; (LOW) e-mails d'invitations sérialisés à tous les membres => chargés seulement pour OWNER/ADMIN ; (LOW) org active non déterministe => départage par id. Isolation RLS de `Invitation` testée explicitement (règle 2) ; tests bootstrap (acceptation, re-validation), service (dernier OWNER, anti-escalade, invite/révoque). Vérifié : typecheck, lint, tests (db 22 + app 115), build verts. **Suite (inv-2)** : sélecteur multi-organisations dans la barre + bascule d'org active (session Auth.js), pour l'utilisateur membre de plusieurs organisations. **Décision d'archi assumée à revoir si besoin** : la re-validation par requête remplace le « zéro aller-retour » du socle (compromis sécurité/latence).

**Correctifs analyse ensoleillement + confiance (retour porteur après test prod) : LIVRÉE (`feat/ombres-vegetation-et-bascule-analyse`, PR à venir).** Quatre points du dernier retour. (1) **Ombres de la végétation qui « remplissaient » la cuvette** : `buildingShadow` prenait l'**enveloppe convexe** de l'empreinte projetée ; pour une forêt concave/étendue ceinturant les hauteurs d'un bassin, l'enveloppe convexe comble le creux et ombre tout le bassin toute la journée (« comme si il n'y avait jamais de soleil direct »). Corrigé par un **balayage de Minkowski** (`sweptShadow` : union `@turf/union` de l'empreinte, sa translation et les quadrilatères d'arêtes, décimée au-delà de 48 sommets) qui **épouse l'emprise réelle** ; le **bâti compact garde l'enveloppe convexe** (correcte pour lui) via `sweep:false`, la **canopée** passe en `sweep:true`. Types élargis `Polygon|MultiPolygon`, repli convexe si l'union échoue. Test « fer à cheval » : l'ancien tracé recouvre le centre du creux, le balayage non. Recalcul des ombres **coalescé sur une frame** (`requestAnimationFrame`) pour ne pas jank quand le curseur d'heure émet des `onChange` en rafale (l'union par canopée est lourde). (2) **Bascule Plan/Satellite masquée pendant l'analyse** : elle **reste visible** ; l'effet du mode soleil distingue « entrée/changement de sélection » (fetch + cadrage) de « rechargement de style » (repeuple depuis `sunDataRef` sans re-fetch ni re-cadrage caméra, via `sunFetchedKeyRef===centroidKey`), et `mapReady` est ajouté aux deps de l'effet de peinture pour réappliquer opacité/teinte/hillshade après `setStyle`. La surcouche 3D survit donc à la bascule de fond. (3) **Retour en arrière** : « Quitter l'analyse » devient un **bouton bordé** (flèche ←), repérable. (4) **« Trois points » mystérieux sur les cartes de la fiche** : c'était `ConfidenceDots` (3 points sans libellé) pris pour un menu cliquable ; ajout d'un **libellé visible « Confiance »** + `title` dans `DataBlock` (conteneur `<div>` pour un HTML conforme). Règles 1/3 tenues : l'ombre reste un rendu « méthode simplifiée » documenté, canopée « approximée », bâtiment sans hauteur non ombré. Vérifié : typecheck, lint, tests (app 102 + ui 59), build verts ; revue adversariale multi-lentilles (2 findings confirmés corrigés : jank curseur via rAF, `<div>`-dans-`<span>` ; 3 réfutés). **Pas un bug** : ajouter un e-mail dans l'organisation = la PR invitations (inv-1/inv-2), slice séparé à venir.

**Section profil (compte + projet + organisation), core : LIVRÉE (PR à venir, `feat/profil`).** Dernier point du retour porteur (« il manque la section profil où on retrouve le projet »). Nouvelle page `/profil` sous le shell : **Compte** (nom, e-mail, organisation, rôle, depuis la session) ; **Projet** éditable (nom, budget max, surface min/max, type de maison, consentement de partage) via `POST /api/projet`, en réutilisant les champs de l'onboarding (le projet cesse d'être éditable seulement à l'onboarding) ; **Organisation** (nom renommable par OWNER/ADMIN via `PATCH /api/org`, + liste des membres). La bulle de compte et le lien « Profil » de la barre pointent vers `/profil`. Nouveau service `modules/organisation/service.ts` via les clients **scopés RLS** (`forOrg`/`withOrg`, jamais le client `admin`) : l'isolation multi-tenant est imposée par Postgres (règle 2), pas seulement par le code (User a bien une policy RLS via Membership, couverte par le test d'isolation de `packages/db`) ; `orgId` vient toujours de la session ; rename réservé OWNER/ADMIN côté serveur (403 sinon). Membre sans nom/e-mail affiché honnêtement (règle 3). Vérifié : typecheck, lint, 99 tests, build verts. **Décidé pour la suite (PR invitations)** : pré-autorisation par e-mail (au login Pocket ID, rattachement si l'e-mail correspond à une invitation) + **multi-org avec sélecteur** ; nouveau modèle `Invitation` + migration + gestion des rôles. À cadrer/livrer ensuite.

**Cadastre en surcouche unique à paliers (retour porteur) : LIVRÉE et mergée (PR #34).** Refonte des fonds de carte de l'explorer pour se caler sur le viewer cadastre officiel. Le **cadastre devient une surcouche UNIQUE** (`ensureCadastreOverlay`) via les **tuiles vectorielles PCI IGN** (`data.geopf.fr/tms/1.0.0/PCI`, sans clé, CORS ouvert), posée sur les deux fonds, stylée par **paliers de zoom** : départements (dézoomé, z<11) → communes + libellés `nom_com` (z11-16) → parcelles (z15+) → numéros via le point `localisant` (z17+, limite des tuiles). Attributs (`idu`, `numero`, `contenance`) interrogeables. Choix porteur : paliers naturels du PCI (pas de couche « sections » polygonale, absente des tuiles). **Fond plan** : son cadastre intégré est **masqué** (`visibility:none` sur `parcellaire_*`/`toponyme_parcellaire_*`) pour éviter le doublon, teinte Veriterra et **adresses conservées** (choix porteur) ; ancien `ensurePlanCadastre` (numéros PR#29) retiré, remplacé par le PCI. **Fond satellite** : cadastre raster retiré, ortho seule ; ses glyphs basculés sur l'endpoint IGN (`.../fonts/...`, sert `Source Sans Pro Regular`) pour que les libellés cadastre s'affichent aussi sur le satellite (l'ancien demotiles renvoyait 404). Résultat : **basculer plan/satellite ne change que l'image de fond**, le cadastre reste identique par-dessus. Le hillshade (mode soleil) inséré sous le cadastre pour ne pas le masquer. **La surcouche est partagée par les 3 vues** (explorer, fiche Vue Soleil, dashboard) : `applyVeriterraPlanTint` redevient une pure teinte, et c'est `ensureCadastreOverlay` qui couple le masquage du cadastre natif du plan + la pose du PCI, appelée dans les 3 (corrige une régression détectée en revue où seul l'explorer posait la surcouche, faisant disparaître le cadastre de la fiche et du dashboard). Vérifié : tuiles PCI + glyphs IGN testés en direct (200, CORS) ; typecheck, lint, 99 tests, build verts ; revue adversariale (1 majeur corrigé, 1 mineur corrigé). À régler en live avec le porteur : lisibilité des traits de cadastre sur le satellite (ton medium, pas de halo possible sur une ligne). **Suite** : section profil.

**Relief permanent dans l'explorer + ombres nocturnes en fondu complet (retour porteur) : LIVRÉE et mergée (PR #33).** Deux points : (1) **relief toujours dispo** : le MNT (`setTerrain`, MNT Terrarium, exagéré 1,2x) est désormais chargé en **permanence sur les deux fonds** (plan + satellite), rejoué dans `installOverlays` à chaque chargement de style (survit à la bascule de fond) ; la **carte reste à plat par défaut** (pitch 0, lisible en 2D) et on l'incline pour voir le relief (NavigationControl passé en `visualizePitch: true` pour le compas/pitch) ; l'analyse d'ensoleillement continue de pitcher automatiquement et ne retire plus le terrain en sortie. Choix validé par le porteur (« relief chargé, carte à plat par défaut »). (2) **ombres nocturnes** : correction du comportement précédent (qui gardait une ombre faible la nuit) : l'ombre **disparaît complètement la nuit** (aucune géométrie, `sunShadowsFor` ne clampe plus l'altitude ; opacité 0 sous l'horizon) et **réapparaît en fondu au lever** (`shadowFadeOpacity` : 0 au ras de l'horizon, fondu smoothstep jusqu'à 20° de hauteur, max 0,32). Constante `SUN_TERRAIN_SOURCE` renommée `TERRAIN_SOURCE`, `ensureSunTerrain`/`removeSunTerrain` remplacées par `ensureTerrain` (permanent). Rendu, pas de la donnée (règles 1/3). Tests purs mis à jour. Vérifié : typecheck, lint, 99 tests, build verts. **Suite (PR-C)** : refonte cadastre en surcouche unique à paliers (départements → communes → parcelles + numéros) via les tuiles vectorielles PCI IGN (sans clé, CORS ouvert), fond plan/satellite ne changeant que l'image ; puis section profil.

**Polish ensoleillement + ombrage du relief (retour porteur après test natif) : LIVRÉE et mergée (PR #32).** Le porteur a validé le rendu natif (« beaucoup mieux ») et demandé des ajustements. Livrés : (1) **canopée revue à la baisse** (bois/forêt 12→10 m, broussaille 3→2 m ; les emprises OSM englobent lisières et trouées, l'ombre était sur-évaluée) ; (2) **bouton reset** (aujourd'hui, 14:00) et **boutons solstices/équinoxes** (`seasonMarks`, saut aux extrêmes de la course du soleil d'un clic) ; (3) **fondu de l'ombre** au lieu du on/off à l'horizon : l'altitude solaire est bornée en bas pour le CALCUL (l'ombre reste orientée à l'azimut réel au crépuscule/la nuit) et sa **visibilité suit un fondu** (`shadowFadeOpacity`, plancher 0,06 la nuit, croissant avec la hauteur du soleil, « mappe » l'intensité) ; (4) **ombrage du relief piloté par le soleil** : calque `hillshade` natif MapLibre sur le MNT (sous les surcouches), dont la **direction suit l'azimut** et l'**exagération** la hauteur du soleil (`hillshadeExaggeration`) ; ce n'est pas une ombre projetée à distance (choix validé face au chantier R&D des vraies ombres portées du relief), mais les versants à contre-jour s'assombrissent en temps réel. Noms de propriétés hillshade vérifiés contre MapLibre 5.24. Rendu, pas de la donnée (règles 1/3) : nuit clairement libellée, canopée toujours « approximée ». Helpers purs testés (`shadowFadeOpacity`, `hillshadeExaggeration`, `seasonMarks`, clamp nocturne). Vérifié : typecheck, lint, 99 tests app, build verts. **Suite (PR-B)** : relief toujours dispo dans l'explorer (à plat par défaut, on incline pour voir) + refonte cadastre en surcouche à paliers (parcelles → sections → communes, comme le viewer officiel, pour que seul le fond bascule en satellite) ; puis section profil.

**Refonte ensoleillement, retour au rendu natif après test porteur : LIVRÉE et mergée (PR #31).** Le porteur a testé la version deck.gl C1 en prod : rendu cassé (bâtiments flottants, façades blanches à triangles noirs = passe d'ombres expérimentale `_SunLight`/`_shadow` déraillée, aucune ombre au sol, sliders qui plantent). Décision validée : **abandon de deck.gl**, retour à la « techno d'avant » (la même qu'utilise déjà `sun-map.tsx` sur la fiche, robuste) mais **portée EN PLACE dans l'explorer** et enrichie. Rendu désormais **100 % natif MapLibre** : relief `setTerrain` (MNT Terrarium, exagéré 1,2x, disclosé), **bâtiments et canopée en `fill-extrusion`** (calés sur le terrain, plus de flottement ni de triangles noirs), **ombres au sol projetées par Turf** (`shadows.ts`, longueur = hauteur/tan(altitude), direction opposée au soleil, règle 1) qui **bougent avec les sliders heure ET saison**, sous l'**ambiance jour/nuit** (`ambience.ts` : ciel `setSky`, teinte des façades et des ombres, voile DOM chaud). Bâti et **canopée végétale approximée** (OSM, hauteur par type, jamais une mesure, règle 3) projettent leurs ombres ; un bâtiment sans hauteur n'est pas extrudé et ne projette pas d'ombre (règle 3). Endpoint `/api/vegetation` (Overpass, durci comme `/api/buildings`) conservé. **deck.gl entièrement retiré** (deps `@deck.gl/*` désinstallées, `sun-scene.ts` supprimé, bundle allégé). Nouveau helper pur testé `lib/sun/sun-render.ts` (extrusion + ombres agrégées bâti+végétation, 3 tests). `sun-map.tsx` (fiche) inchangé. **Hors périmètre (fast-follow)** : ombres portées PAR le relief (le seul vrai plus de deck.gl, chantier R&D non bloquant), unification `sun-map`/explorer. Vérifié : typecheck, lint, 96 tests app + enrichment + worker, build verts.

**Lisibilité carte (retour porteur) : LIVRÉE et mergée (PR #29).** Quatre points : (1) **numéros de parcelle en vue plan**, absents jusqu'ici car le style gris IGN ne les rend pas : nouvelle couche `symbol` sur la source `plan_ign`, source-layer `toponyme_parcellaire_parcelle` (champ `texte`, la source-layer qui porte réellement le numéro, vérifiée via les métadonnées IGN), dès z16, halo blanc, police reprise d'un calque symbole existant (fontstack littéral uniquement) pour rester dans les glyphs servis ; (2) **contraste du plan relevé** : liseré cadastral assombri (`#B7BFD3` → `#8A93AD`) et épaissi, textes d'adresse `#98A0B0` → `#6C7488` (WCAG) ; (3) **sélection lisible sur satellite** : liseré de contraste sombre (`selection-casing`) sous le trait ambre, qui se noyait sur l'orthophoto ; (4) **cadastre raster satellite allégé** (`raster-opacity` 0.85 → 0.6). Revue adversariale : 0 HIGH/MEDIUM, LOW documentés (à vérifier en live : présence du champ `texte` sur les tuiles ; les numéros apparaissent aussi sur les cartes dashboard et soleil, cohérent avec le liseré déjà partagé). Vérifié : typecheck, lint, build verts (changement visuel, vérification manuelle du rendu). Reste : refonte « Analyser l'ensoleillement » (deck.gl vraies ombres relief+bâti+végétation, en place dans l'explorer, slider saison), section profil.

**Correctifs bugs (retour porteur) : LIVRÉE et mergée (PR #28).** Quatre bugs corrigés : (1) **502 à la création** de terrain alors que la ligne était persistée : l'enfilement de l'enrichissement (effet de bord post-commit) devient best-effort borné (Promise.race + délai 4 s), le POST renvoie 201 même si Redis est indisponible (blocs laissés PENDING, un « Actualiser » relance). (2) **Suppression de terrain** (absente) : `deleteTerrain(orgId, id)` scopé RLS (cascade parcelles/enrichissements/documents, nettoyage best-effort des objets S3), route `DELETE /api/terrains/[id]`, bouton « Supprimer le terrain » avec confirmation sur la fiche. (3) **Parcelles qui disparaissaient au retour en vue plan** après un aller-retour satellite : réinstallation des calques au premier `styledata` après `setStyle` (plus de gate `isStyleLoaded` qui ratait l'instant). (4) **Zonage PLU jamais visible** : résumé du zonage ajouté dans l'onglet Aperçu (le détail restait derrière l'onglet Enrichissement) ; robustesse PLU : `fetchPlu` ne dépend plus que de `zone-urba` pour le réessai (municipality/document = données molles), et `safeGet` borne chaque appel par un timeout (`AbortSignal.timeout`) pour qu'une source lente n'affame plus le PLU. Revue adversariale (sécurité + correction) : 0 finding. Vérifié : typecheck, lint, 86 tests app + 51 enrichment (dont 2 nouveaux PLU) + 6 worker, build verts. Reste du retour porteur à traiter : lisibilité carte (numéros de parcelle, contraste, sélection satellite), refonte « Analyser l'ensoleillement » (deck.gl vraies ombres relief+bâti+végétation, en place dans l'explorer, slider saison), section profil.

**Passe de cohérence UX (shell, fiche/dashboard, explorer, soleil) : LIVRÉE (PR #24 à #27 mergées).** Suite au retour du porteur (app « flat » et incohérente, cliquer un terrain donnait l'impression « d'une page complètement différente »), un audit multi-agents a confirmé la cause racine : aucun shell applicatif partagé, barre de nav réécrite en inline deux fois et absente de la fiche, dashboard/explorer 100 % en hex inline pendant que seule la fiche utilisait les tokens. Livraison en 4 slices (PR1 shell → PR2 fiche+dashboard alignés → PR3 explorer → PR4 soleil ambiance+relief).
**PR4 — Soleil, ambiance + relief + vue soleil explorer (`feat/soleil`) : LIVRÉE (PR à venir).** **Ambiance jour/nuit** pilotée par la seule hauteur du soleil : helper pur `lib/sun/ambience.ts` (interpolé entre ancres d'altitude, 6 tests) appliqué via `setSky` (ciel), teinte des façades et des ombres (`setPaintProperty`) et un voile DOM (soft-light au crépuscule, multiply la nuit). Voile purement visuel, jamais de la donnée (règles 1/3), physique des ombres inchangée. **Relief** via `map.setTerrain` avec le **MNT Terrarium** (AWS Open Data, tuiles libres, CORS vérifié, exagération 1,2x, attribution affichée) : le bâti et les ombres reposent enfin sur le relief réel. **Vue soleil dans l'explorer** : nouvel endpoint `GET /api/buildings?lon=&lat=&radius=` (bâtiments BD TOPO par bbox, non lié à un terrain car donnée publique IGN, mais **auth + limite de débit par organisation + coordonnées arrondies à ~110 m + rayon/latitude bornés** contre l'amplification), `SunMap` découplé (prop `buildingsUrl` au lieu de `terrainId`, prop `fill`), bouton « Vue soleil » ouvrant le simulateur en modale plein écran (montée seulement à l'ouverture, `role=dialog`, `aria-modal`, fermeture Échap). Revue adversariale (3 relecteurs : sécurité, correction, règles) : 1 MEDIUM (amplification de l'endpoint) et plusieurs LOW corrigés (validation lon/lat manquants, latitude dégénérée aux pôles, libellés honnêtes sur la projection simplifiée des ombres et l'exagération, copie de la fiche à jour, a11y de la modale). Vérifié : typecheck, lint, 86 tests, build verts.

**PR3 — Explorer, quick wins + reskin (`feat/explorer-polish`) : LIVRÉE et mergée (PR #26).** Le **panneau de détails n'apparaît qu'après sélection** d'au moins une parcelle (fin du formulaire vide et du « Surface totale, donnée indisponible » permanent ; l'état adresse/prix/notes vit dans le parent, rien n'est perdu si on désélectionne). **Zoom d'adresse adaptatif** selon la granularité BAN (`BanFeature.type` : commune 13, lieu-dit 14, rue 16, numéro 17, défaut 15) au lieu de 18 fixe (fin du « ça zoome trop » sur une ville). **Panneau reskiné en tokens** (`Input`/`Button`, `rounded-xl`/`shadow-lg`/`border-border`, plus d'hex inline, focus visible partout), même langage visuel que la fiche et le dashboard. Revue adversariale (2 relecteurs) : 3 findings LOW corrigés (extraction de `zoomForBanType` dans `ban.ts` + 3 tests unitaires ; encart d'erreur aligné sur la famille `destructive` comme les autres écrans, cohérent en thème sombre ; contraste de la ligne de source relevé à `text-neutral-500`, WCAG AA). Vérifié : typecheck, lint, 77 tests, build verts.

**PR2 — Fiche alignée + dashboard/tableau en tokens (`feat/fiche-dashboard-tokens`) : LIVRÉE et mergée (PR #25).** En-tête de fiche façon maquette : pin de statut + nom + adresse + grande `ScoreGauge` « Score global sur 100 » + résumé sourcé (Surface / Prix / Prix au m² / Réf. cadastrale) en `DataBlock`, chaque tuile portant source/date/confiance et marquée indisponible si la donnée manque (règle 3) ; le résumé quitte l'onglet Aperçu (plus de doublon), `ScoreCard` devient « Score par catégorie » sans double jauge. Dashboard et tableau migrés des styles inline hex vers les tokens et composants (`Card`/`Button`/`StatusPin`/`Badge`, ombre sm, hover de ligne) ; dashboard recentré (`max-w-6xl`) pour s'aligner sur la fiche. Tri du tableau **par clic sur les en-têtes de colonnes** (chevron ▲/▼, plus de select box) : `sortTerrains` étendu (paramètre `direction` + clé `label`), valeurs manquantes **toujours en fin dans les deux sens** (règle 3), 5 nouveaux tests. Revue adversariale (2 relecteurs) : 2 findings MEDIUM corrigés (en-tête de tri redéfini au rendu qui perdait le focus clavier ; focus de ligne invisible, WCAG 2.4.7). Vérifié : typecheck, lint, 25 tests, build verts. Note : le tri par clic remplace la select box (demande du porteur), les modes « récents » (ordre par défaut) et « prix au m² » ne sont plus des options explicites.

**PR1 — Shell commun (`feat/app-shell`) : LIVRÉE et mergée (PR #24).** Groupe de routes authentifié `(app)/` (URLs inchangées) portant un shell unique : `AppShell` + `AppTopBar` (client, `usePathname` pour la pastille active, tokens Tailwind, aucun hex inline, bulle d'initiales de compte + déconnexion via Server Action). Logo factorisé (`VeriterraMark`, remplace 4 copies) et statuts portefeuille centralisés (`modules/terrains/status.ts`, remplace 3 définitions dupliquées, branché sur la fiche). Dashboard, fiche et explorer passent sous la même barre : la fiche cesse d'être une page à part. Vérifié : lint, typecheck, build verts ; routes identiques (`/dashboard`, `/terrains/[id]`, `/terrains/nouveau`). **Hors périmètre PR1 (fast-follow)** : migration des `style={{}}`/hex du dashboard vers `Card`/`Badge`/`StatusPin`, en-tête de fiche façon maquette, tri par clic sur colonnes, quick wins explorer, ambiance/relief soleil.

**Tranche 0 — Socle : FAITE, mergée (PR #1-3) et DÉPLOYÉE en prod** via Arcane sur `veriterra.lukas-houille.com` (image GHCR, Caddy TLS, `migrate` one-shot exit 0). Couvre US-0.1 (auth OIDC Pocket ID), US-0.2 (multi-tenant + RLS + test d'isolation), US-0.3 (stack déployable).

**Tranche design system — `@veriterra/ui` : FAITE et mergée (PR #4).** CI verte (dont e2e OIDC), revue en contexte neuf GO.

Bibliothèque de composants du design system Veriterra, construite comme **package syncable vers claude.ai/design** (le run `/design-sync` viendra ensuite). 11 composants (core + signature), tokens déplacés dans le package (source de vérité unique), polices vendorisées en `@font-face`. Voir la section dédiée plus bas.

**Rebranding fait : la marque est Veriterra** (renommage complet, code compris : packages `@veriterra/*`, rôle DB `veriterra_app`, DB `veriterra`, image, repo, docs). Design system : `docs/design-system.md` (spec), et désormais son **implémentation réelle** dans `packages/ui` (indigo `#2F3B6E` primaire, accent soleil `#DB9B2C`, polices Archivo + Spline Sans Mono).

**Tranche 2, Évaluer (enrichissement sourcé) : EN COURS.** Slice 2 (prix DVF) livrée (`feat/enrichissement-dvf`) : bloc `PRIX_DVF` via l'API Etalab DVF, estimation médiane €/m² **des seules ventes de terrain à bâtir** (code cadastral AB, sans bâti ni mélange agricole) + fourchette (quartiles) + nombre de comparables + écart au prix demandé, hors couverture (Alsace-Moselle/Mayotte) et comparables insuffisants traités en indisponible explicite. Worker restructuré en blocs indépendants, réessai sur toute panne transitoire (même partielle multi-sections). Revue adversariale : 9 findings corrigés (le principal : ne plus mélanger terres agricoles et terrains à bâtir dans la médiane).

**Tranche 3, Scoring et comparaison, MVP : LIVRÉE** (`feat/scoring`, PR à venir). **Score hybride /100** (US-3.1) dérivé des données déjà sourcées (règle 1) et relatif au projet, **calculé à la lecture** (aucune migration). Moteur pur `scoring.ts` : 8 critères de la scorecard (prix 20, constructibilité 15, risques 15, ensoleillement 15, pente 10, services 10, trajet 10, tension 5), notés depuis les blocs d'enrichissement + le projet. **Critère sans donnée = non évalué (null, jamais 0, règle 3)** ; global = moyenne pondérée **renormalisée** sur les seuls critères évalués (« N/8 évalués »). Ensoleillement via l'exposition (PENTE) en proxy documenté (score détaillé US-4.3). **Alertes rouges** (US-3.4) : non constructible (PLU A/N), inondable, hors DVF ; elles pèsent via leur critère sans exclure le terrain. **Affichage fiche** (US-3.2) : `ScoreGauge` global + détail par critère (barre + base sourcée) + alertes. **Tableau comparatif** (US-3.3) : colonne Score + tri par score (non évalués en fin) + badge d'alertes sur le dashboard, via `listTerrainsWithScores` (un seul findMany des blocs scopé tenant, pas de N+1). Vérifié en live : terrain prix/risques/pente/PLU => score 85, 5/8 critères évalués. **Hors périmètre (fast-follow)** : override manuel + modèle `Score` persisté (US-3.1), radar SVG, filtres avancés du tableau.

**Analyse solaire 3D, MVP (US-4.1) : LIVRÉE** (`feat/analyse-solaire`, PR à venir). Nouvel onglet **« Soleil »** sur la fiche : vue **3D pitchée** de la parcelle (contour ambre), **bâtiments voisins extrudés** depuis l'**IGN BD TOPO** (hauteur photogrammétrique autoritative, rayon 250 m), et **ombres portées au sol** calculées et bougeant avec des curseurs **date/heure**. Ombres = polygones projetés via **Turf** (hauteur/tan(altitude), direction opposée au soleil), position solaire via **SunCalc** ; léger, déterministe, explicable (règle 1), **sans deck.gl**. Bâtiment sans hauteur = exclu des ombres et signalé (règle 3), jamais extrudé à 0 silencieusement. Nouvelles dépendances (client) : `suncalc`, `@turf/*`. Socle pur testé (`shadows.ts`), client BD TOPO + route scopée tenant (`/api/terrains/[id]/buildings`), îlot MapLibre monté seulement quand l'onglet est ouvert (Radix démonte l'inactif). Vérifié en live : 159 bâtiments autour de Lyon (158 avec hauteur), ombres plus longues au soleil bas (hiver 16h30 à 3°) qu'à midi d'été (68°). **Hors périmètre (US-4.2/4.3)** : relief/MNT, ombres bâtiment-sur-bâtiment, timelapse, halo annuel, heatmap, maison projetée. Convention subtile notée : ce build de `suncalc@2.0.0` renvoie des degrés/azimut nord (pas les radians/sud de l'API historique), verrouillé par un test.

Slice 5 livrée (`feat/enrichissement-plu`) : bloc **PLU** via l'**IGN API Carto GPU** (Géoportail de l'Urbanisme), **sans IA**. Récupération déterministe du **zonage** (type U/AU/A/N + libellé local + description), du **document d'urbanisme** (PLU/PLUi, nom, date de validité) et d'un **lien vers le règlement** (téléchargement par partition sur le Géoportail de l'Urbanisme) ; 3 endpoints GPU en parallèle (zone-urba + municipality + document). Commune au **RNU** ou zonage non téléversé, hors couverture : indisponible explicite (règle 3, jamais une zone par défaut). Panne transitoire GPU tout-ou-rien : bloc ERROR (jamais un faux « pas de PLU »). Confiance MOYENNE (données « source » non normalisées CNIG). Aucune migration (enum `PLU` existant). L'**extraction IA du règlement** (hauteur, emprise, reculs, avec citation d'article) reste une slice ultérieure sur ce socle. Vérifié en live : Lyon PLUi zone U (UPp), coteau Cantal PLUi zone N, océan hors couverture.

Slice 4 livrée (`feat/enrichissement-services`) : bloc **SERVICES** via **Overpass (OpenStreetMap)**. Autour du centre de la parcelle (rayon 1,5 km), distance au plus proche et nombre pour **écoles**, **commerces** et **transports**. Overpass exige un User-Agent (sinon 406) et peut renvoyer 429/504 (transitoire, `safeGet` traite désormais 429 comme réessayable). Aucune migration (enum `SERVICES` existant). « Aucun dans le rayon » est une réponse réelle (nearest null, pas un 0 silencieux, règle 3) ; confiance MOYENNE (complétude OSM inégale). Vérifié en live : Lyon centre écoles 90 m / commerces 80 m / transports 90 m ; zone rurale (Cantal) aucun service dans le rayon.

Slice 3 livrée (`feat/enrichissement-pente`) : bloc **PENTE** via le **RGE ALTI (IGN)**. On échantillonne 5 altitudes (centre + est/ouest/nord/sud à 20 m) en une requête, puis on dérive par différences finies la **pente** (%, degrés), l'**exposition** (8 directions, cap boussole de la direction aval) et l'altitude. Terrain plat (< 0,5 %) : exposition non marquée. Hors couverture (sentinelle -99999) : indisponible explicite (règle 3). Confiance MOYENNE (altitude autoritative, pente estimée par échantillonnage). Aucune migration (l'enum `PENTE` existait déjà). Vérifié en live : Alpes 42,6 % / 23°, coteau Côte-Rôtie 8,3 % exposition Sud-Est (cohérent), plaine ~1 %, océan hors couverture.

Slice 1 livrée (`feat/enrichissement-georisques`) : le job `enrichTerrain` n'est plus un stub, il récupère les **risques Géorisques** (argile, inondation, radon, sismicité) et écrit un bloc `EnrichmentBlock` sourcé, via un nouveau package partagé `@veriterra/enrichment` (client + cache Redis). Table + enums + migration RLS (ENABLE/FORCE, garde M3, isolation par-table). Fiabilité BullMQ (essais/backoff, jobId idempotent). La fiche affiche les blocs pilotés par le modèle (provenance/date/confiance, `AlertChip` par sévérité, préchargement non bloquant US-1.4, squelette PENDING, bouton Actualiser + polling borné). Distinction **panne transitoire** (bloc ERROR réessayable, non mis en cache) vs **absence légitime** (UNAVAILABLE terminal, règle 3). Revue adversariale multi-agents : 9 findings corrigés (le principal : ne plus figer une panne de source en faux « aucun risque »).

**Pièces jointes aux terrains (photos US-5.3 + documents US-5.8) : LIVRÉE** (`feat/terrain-documents`, PR #17). Socle **stockage objet MinIO auto-hébergé** (S3-compatible), sur le **réseau interne uniquement** (jamais exposé à Caddy ni au navigateur). **Upload et download par proxy applicatif** : l'app est le seul intermédiaire, chaque accès est ré-authentifié (session) et re-scopé tenant (RLS), aucune URL de stockage n'est exposée. Modèle `TerrainDocument` scopé tenant (RLS ENABLE+FORCE, garde M3, isolation par-table), enums `DocumentKind`/`DocumentType`, migration hand-écrite. Client `@aws-sdk/client-s3` + lib `validate.ts` **pure** : liste blanche PDF/JPEG/PNG/WebP (pas de SVG), taille max configurable, **sniff des octets d'en-tête** (le contenu doit correspondre au type déclaré, règle 1), assainissement du nom, clé objet opaque. Routes POST/GET/DELETE, download durci (nosniff, CSP sandbox, disposition inline pour les photos / attachment sinon, `filename*` RFC 5987). Onglet Documents (grille photos, liste documents avec type/taille/provenance/date, suppression, état vide règle 3). MinIO dans les deux composes (interne en prod) et en CI (round-trip réel). Revue adversariale multi-agents (3 lentilles : upload/sécurité, isolation tenant, correction) : isolation **SOUND**, aucun HIGH ; 3 findings corrigés : (a) DoS upload chunked (pas de Content-Length, borne contournée puis `formData` bufferisait tout) corrigé par une lecture bornée du flux (`readCappedBody`) ; (b) `maxUploadMbForDisplay` pouvait throw sur config S3 partielle et casser toute la fiche, corrigé par try/catch repli 25 ; (c) encodage RFC 5987 du `filename*` complété. **Antivirus ClamAV différé** (avec US-8.4), ainsi que miniatures, conversion HEIC et purge des objets orphelins.

**Slice UX exploration et portefeuille : LIVRÉE** (`feat/exploration-ux`, PR #18). Quatre quick wins issus d'idées du porteur : bouton « Réinitialiser la sélection » sur la carte de création (US-1.10) ; découpage cadastral vectoriel visible dès le zoom 13 au lieu de 15 (US-1.12) ; infobulle au survol d'un pin du dashboard avec libellé, surface et prix au m² (US-5.2, libellé échappé anti-XSS, contenu rafraîchi entre pins voisins) ; recherche et tri (récents, surface, prix total, prix au m²) de la liste des terrains via un îlot client sur logique pure et testée (US-5.9), un prix absent restant listé mais jamais trié comme 0 (règle 3). Revue adversariale (correction/régression + XSS de l'infobulle) : escaping sain, 1 finding LOW corrigé (infobulle qui gardait l'ancien terrain entre deux pins voisins). Le fond satellite « plus moderne » (US-1.11) reste en attente d'une décision de fournisseur de tuiles.

**Tranche 1 — Projet et exploration : LIVRÉE et déployée.** Landing publique (PR #10), onboarding court + modèle `Projet` (RLS, flag de consentement de partage) (PR #9), écrans recalés sur les maquettes designées (PR #11), création de terrain de bout en bout (adresse BAN → cadastre → clic parcelle → fiche persistée → dashboard carte, PR #7-8). Puis, sur `feat/explorer-surface-fond-carte` : **recherche par surface approchée ±X m² (US-1.6)**, **fond de carte modernisé** (plan vectoriel « Positron Veriterra » recoloré à chaud depuis le plan IGN + bascule Plan/Satellite, cadastre en calque, base 3D-ready), et **pins colorés par statut cliquables** sur le dashboard (visibles même dézoomés). Revue adversariale multi-angles passée : 10 findings corrigés, dont la course `setStyle`/diff asynchrone de MapLibre (bascule de fond qui effaçait les calques), un faux négatif quand la zone est tronquée, et l'exclusion des parcelles sans contenance connue (règle 3).

### Vérifié de bout en bout
- `pnpm lint`, `pnpm typecheck`, `pnpm build` : verts.
- `pnpm test` : 9/9 — isolation RLS 6/6 (dont fail-closed + contrôle négatif `admin`), bootstrap orga 2/2, pipeline ping 1/1.
- `pnpm --filter @veriterra/app test:e2e` (local) : accès protégé → redirection `/sign-in` au vert. Flux OIDC complet via mock : câblé en CI (`E2E_OIDC=1`).
- `docker compose up` : `db`, `redis`, `app`, `worker` **healthy** ; `migrate` exit 0 ; `GET /api/health` → 200 `{"status":"ok","checks":{"db":true,"redis":true}}` ; `/` sans session → 307 `/sign-in`.
- En base : `veriterra_app` sans `rolsuper`/`rolbypassrls`, RLS **ENABLE+FORCE** sur `Organisation` et `Membership`, policies `tenant_isolation` présentes, PostGIS OK.

## Tranche design system — `@veriterra/ui`

Nouveau package workspace `packages/ui` (`@veriterra/ui`), pensé pour être **syncable vers claude.ai/design** (package-shape de `/design-sync`).

- **11 composants** (fidèles à `docs/design-system.md`, chacun avec son type `*Props` exporté et ses tests) :
  - Primitives (§6) : `Button` (CVA + variantes default/secondary/ghost/destructive, tailles sm/default/lg, `asChild` via Radix Slot), `Card` (+ Header/Title/Description/Content/Footer), `Badge` (5 statuts), `Input`, `Tabs` (Radix).
  - Signature (§7) : `ConfidenceDots`, `DataBlock` (bloc « donnée sourcée », bascule `unavailable` → `UnavailableState`), `UnavailableState`, `ScoreGauge` (anneau SVG), `AlertChip`, `StatusPin`.
- **Tokens = source de vérité unique** : déplacés de `app/globals.css` vers `packages/ui/src/styles/theme.css` (`--vt-*`, variables shadcn light/dark, mapping `@theme inline`, `.font-data`). L'app les `@import` + `@source` vers `packages/ui/src`.
- **Polices vendorisées** : `@fontsource-variable/{archivo,spline-sans-mono}` copiées en woff2 sous `packages/ui/src/styles/fonts/`, servies par `@font-face` dans le thème. L'app **abandonne `next/font`** (parité exacte app/bundle design-sync, plus de dépendance Google Fonts au build).
- **Build** : `tsc` (émet `dist/**` ESM + `.d.ts`) + `@tailwindcss/cli` (compile `dist/veriterra.css` = tokens + `@font-face` + utilitaires des composants) + copie `dist/fonts`. Exactement ce que `/design-sync` (package-shape) consomme.
- **Consommation par l'app** : en **source** via `transpilePackages` (comme `@veriterra/shared`) ; imports relatifs internes **extensionless** (obligatoire pour Turbopack, cf. écart ci-dessous). `dist/` gitignoré, uniquement pour design-sync.
- **Vérifié** : `pnpm --filter @veriterra/ui build/test/typecheck` (59 tests), `pnpm lint`, `pnpm --filter @veriterra/app build` (Next, thème du package + composants), suite complète `pnpm test` (db 8 · ui 59 · app 2 · worker 1). Sélecteurs e2e préservés (texte des boutons inchangé) → e2e validé par la CI (mock OIDC).
- **Revue en contexte neuf : GO** (re-vérifiée empiriquement : package + app buildent/testent au vert). Corrigés au passage : `input.tsx` `ring-ring` → `focus-visible:ring-ring`, `lucide-react` retiré (inutilisé, reviendra avec les icônes), `app` dep en `workspace:*`.
- **Suivis non bloquants** : (a) cible tactile mobile du `Button` `default` à 40px (§9 demande ≥44px en mobile, à arbitrer, ex. `min-h-11 md:h-10`) ; (b) `AlertChip` utilise les hex exacts de §7 (cohérent avec la spec mais pas les classes token comme `Badge`) ; (c) `RadarScore` et la variante prix/score de `StatusPin` (§7) restent en 2e vague.

## Décisions structurantes (validées avec le porteur)

1. **Un user = une organisation**, créée en silence à la 1re connexion (`bootstrapUserOrganisation`). Aucune notion d'orga visible dans le front (pas d'invitation/partage). Le modèle orga + RLS est en place dès maintenant, donc le partage futur n'exigera pas de migration de données.
2. **UI : en attente du design system** (Claude Design, en //). Tranche 0 = socle backend/infra. Pages présentes = placeholders nus jetables (`/sign-in`, `/` protégée, `/api/health`), à remplacer quand les tokens arrivent.
3. **OIDC** : vrai Pocket ID branché par env en dev/prod ; la CI utilise un **mock OIDC** (`navikt/mock-oauth2-server`, `interactiveLogin:false`) pour un e2e auth sans IdP live.
4. **GitHub** : repo à créer ; le push CI→GHCR utilise le `GITHUB_TOKEN` intégré.

## Stack effective (versions épinglées)

Next 16.2.9 (App Router, Turbopack) · React 19.2 · Auth.js `next-auth@5.0.0-beta.31` (JWT, provider OIDC générique) · Prisma 7.8 (client Rust-free wasm + `@prisma/adapter-pg`) · Postgres 17 + PostGIS 3.5 (`imresamu/postgis`, multi-arch) · Redis 7 · BullMQ 5.79 + ioredis 5.11 · Tailwind 4 · TypeScript 5.9.3 · Vitest 4 · Playwright 1.61 · ESLint 10 (typescript-eslint, flat config) · pnpm 9.13 workspaces · Node 22.

Design system (`@veriterra/ui`) : class-variance-authority · clsx · tailwind-merge 3 · lucide-react · @radix-ui/react-{slot,tabs} · @fontsource-variable/{archivo,spline-sans-mono} (vendorisées) · @tailwindcss/cli 4 · @testing-library/react + jsdom (Vitest).

## Architecture d'isolation (le cœur)

- **Deux clients DB** (`packages/db/src/client.ts`) : `prisma` = rôle restreint `veriterra_app` via `DATABASE_URL` (RLS le contraint) ; `admin` = rôle privilégié via `DIRECT_URL` (bypass RLS), réservé au bootstrap auth / seeds / migrations / contrôle négatif des tests.
- **`forOrg(orgId)`** (`packages/db/src/rls.ts`) : extension Prisma `$extends` qui enveloppe chaque opération dans une transaction posant `set_config('app.current_org_id', orgId, true)` (LOCAL, sûr en pooling) avant la requête. Tout accès aux données tenant passe par là.
- **Policies** (`migrations/.../rls`) : `ENABLE`+`FORCE RLS` sur `Organisation`/`Membership`, `USING`/`WITH CHECK` sur `NULLIF(current_setting('app.current_org_id', true), '')::uuid` (fail-closed propre).
- **Worker** : sans session, scope par `job.data.organizationId` via `forOrg`.

## Écarts/notes par rapport au plan (à savoir)

- **Prisma 7** : l'URL de connexion n'est plus dans `schema.prisma` mais dans `prisma.config.ts` ; le client reçoit l'`adapter` (`@prisma/adapter-pg`). Pas de `directUrl` côté schéma.
- **`@veriterra/db` compilé en `dist` + marqué externe dans Next** : le client Prisma 7 (wasm + requires dynamiques) ne se bundle pas. Le client généré est émis en frère de `src`/`dist` (`packages/db/generated`) et importé via `../generated/prisma/index.js` (un seul chemin valable depuis la source ET le build).
- **Worker en `tsx`** (pas de bundling) pour éviter le même écueil Prisma. Le `dev` et le `test` racine buildent `@veriterra/db` d'abord.
- **Next 16** : `middleware` renommé en `proxy` (`app/src/proxy.ts`).
- **Imports relatifs de `@veriterra/ui` : extensionless** (`./button`, `../lib/cn`), PAS `.js`. Turbopack (qui transpile la source du package) ne résout pas `.js` → `.tsx` ; `tsc` et Vitest le font, mais pas le build Next. Convention alignée sur `@veriterra/shared`. Le `dist/` (esbuild de design-sync) résout aussi l'extensionless.
- **Warning Next « multiple lockfiles »** : un `~/package-lock.json` traîne dans le home et Next le choisit comme racine. Sans effet sur le build (vert), à nettoyer côté poste (ou fixer `turbopack.root`).
- **Image Docker unique « grasse »** (toutes deps) ; `app`/`worker`/`migrate` la lancent avec des `command` différentes. Slimming en images séparées (standalone) = à suivre.
- **`pnpm --filter X deploy`** entre en collision avec la commande intégrée pnpm → toujours `run deploy`.
- **Mot de passe `veriterra_app`** par défaut dans la migration RLS (dev). En prod : pré-provisionner le rôle avec un secret fort avant la migration (le `IF NOT EXISTS` ne le recrée alors pas). À durcir.
- **`prisma migrate reset`/`migrate dev`** : Prisma 7 exige le consentement explicite de l'utilisateur (garde anti-IA) et `--schema`. En CI/compose on utilise `migrate deploy` (pas concerné).

## Boucle de travail locale

1. `cp .env.example .env` (déjà fait localement, `.env` gitignoré).
2. `docker compose up -d db redis` (ou tout : `docker compose up`).
3. `pnpm db:deploy` puis `pnpm db:seed` (charge le `.env` via dotenv).
4. `pnpm dev` (app + worker), app sur http://localhost:3000.
5. `pnpm test`, `pnpm test:e2e`, `pnpm lint && pnpm typecheck`.

## Revue de sécurité (sous-agent, contexte neuf)

Verdict : **l'invariant d'isolation tenant tient, aucune fuite ni bypass actif.** Corrigés avant le commit initial :
- **H1** — `User` n'avait pas de RLS alors que le rôle restreint a des droits dessus (fuite PII latente) → policy `User` ajoutée (visible uniquement si membre de l'org courante, via `Membership`) + test.
- **H2** — mot de passe `veriterra_app` codé en dur + variable `VERITERRA_APP_PASSWORD` au commentaire trompeur → variable supprimée, mot de passe dev figé, doc corrigée (prod = pré-provisionner le rôle).
- **M1** — `release.yml` ne dépendait pas de la CI → déclenché par `workflow_run` sur succès de `ci` uniquement.
- **M2** — `admin` retombait en silence sur le rôle restreint si `DIRECT_URL` manquait → plus de fallback (échec franc).
- **M3** — garde de test : échoue si une future table à `organisationId` n'a pas RLS *enabled+forced*.

Suivis tracés (non bloquants) : M5 secrets app avec défauts `change-me` (fail-fast au boot à ajouter) · L1 documenter `withOrg()` pour le raw SQL tenant · L2 `loadServerEnv` mort (à câbler au démarrage) · L3 image single-arch · L4/L4bis vérifier le déclenchement Arcane (label image vs service) · L5 rate-limit `/api/health` au reverse-proxy · L6 traiter Redis comme frontière de confiance (provenance `organizationId`) · L7 révocation rôle/membership vs TTL JWT 1h · L8 `CMD` placeholder. M4 (le proxy protège bien) **vérifié** par l'e2e.

## Prochaine étape

**Tranche 1 : LIVRÉE. Tranche 2 slices 1 (risques Géorisques) et 2 (prix DVF) : LIVRÉES. Pièces jointes (photos + documents, MinIO) : LIVRÉE** (`feat/terrain-documents`, PR #17). Suite :
- **Pente et exposition** (US-2.4) : LIVRÉE (bloc `PENTE`, RGE ALTI).
- **Services de proximité** (US-2.5) : LIVRÉE (bloc `SERVICES`, Overpass/OSM).
- **PLU zonage (US-2.1, récupération sans IA)** : LIVRÉE (bloc `PLU`, API Carto GPU). **Reste l'extraction IA du règlement** (hauteur, emprise, reculs, citation d'article) : slice ultérieure, clé `ANTHROPIC_API_KEY` requise dans le `.env` Arcane.
- **Tranche 2 enrichissement : les 5 blocs déterministes sont livrés** (Risques, Prix DVF, Pente, Services, PLU zonage). Suite possible : **Tranche 3 Scoring et comparaison** (US-3.1 à 3.5), l'**analyse solaire 3D** (US-4.1, feature phare), ou l'**extraction IA du PLU**.
- **Tranche 3 Scoring : US-3.1 override manuel LIVRÉE** (`feat/score-override`). Nouveau modèle `TerrainScoreOverride` persisté (scopé RLS, migration + test d'isolation), moteur `applyOverrides` pur (re-renormalise le global, trace d'origine figée à la pose et préférée au dérivé courant, règle 1), service `set/clear/getScoreOverrides` (via `withOrg`/`forOrg`, 404 cross-org, TOCTOU FK mappée en 404), route `PUT/DELETE /api/terrains/[id]/score-overrides`, îlot client `ScoreCriteriaEditor` sur la fiche. **Prochaine : US-3.3 filtres avancés** (statut, commune, prix) en panneau sur le dashboard.
- **Fond satellite plus moderne** (US-1.11) : en attente d'une décision de fournisseur de tuiles (IGN actuel / Esri World Imagery / fournisseur à clé). À trancher avec le porteur avant implémentation.
- **Suivi documents (non bloquant)** : antivirus ClamAV (avec le partage externe US-8.4), miniatures générées, conversion HEIC, purge des objets orphelins à la suppression d'un terrain, et un cap `request_body` au niveau de Caddy en prod (défense en profondeur au-delà de la borne applicative).

Suivi de dette repéré par la revue (non bloquant) : point représentatif par moyenne des sommets (préférer `ST_PointOnSurface` pour les parcelles concaves/disjointes) ; bloc PENDING jamais résolu détecté seulement côté client (persistance d'un horodatage d'enfilement pour une détection serveur possible plus tard).

Autres chantiers (selon priorité) :
- **`/design-sync`** : le dépôt est syncable (`pkg: @veriterra/ui`, `cssEntry: dist/veriterra.css`). Opération lourde (budget frais recommandé).
- **Durcissement prod** (service public) : `AUTH_SECRET` fort (signature JWT, prioritaire), `POSTGRES_PASSWORD` fort, rotation du rôle `veriterra_app` ; puis image Docker slim, headers de sécurité, rate limiting, fail-fast sur `AUTH_SECRET` placeholder.
- **Features cadrées au backlog** : documents attachés + partage externe (US-5.8 / US-8.4, stockage objet à décider), collaboration à plusieurs sur le projet (US-0.4, modèle `Membership` + RLS déjà posé).
